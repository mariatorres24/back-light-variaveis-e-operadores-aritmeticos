let altura = 1.84;
let peso = 75;
let imc = peso / Math.pow(altura,2);
console.log(`Meu IMC é ${imc}`);

altura = 1.79;
peso = 76;
imc = peso / Math.pow(altura,2);
console.log(`O IMC do exemplo do readme é ${imc}`);

altura = 1.51;
peso = 82;
imc = peso / Math.pow(altura,2);
console.log(`Mais um exemplo de IMC é ${imc}`);