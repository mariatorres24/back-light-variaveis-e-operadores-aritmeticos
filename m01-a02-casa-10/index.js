let x1 = 1
let y1 = 5
let x2 = 3
let y2 = 12

let distancia = (Math.sqrt((Math.pow ((x2 - x1), 2) + (Math.pow((y2 - y1), 2)))).toFixed(2));

console.log(`A distância total entre os dois pontos é de ${distancia} metros.`);