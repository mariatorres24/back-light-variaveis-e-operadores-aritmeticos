const precoTenis = 130;
const valorDesconto = 50;

const porcentDesconto = ((valorDesconto*100)/precoTenis);

console.log(`A porcentagem do desconto terá que ser de ${porcentDesconto.toFixed(1)}%`);
