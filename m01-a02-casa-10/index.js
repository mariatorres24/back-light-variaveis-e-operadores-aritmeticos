const x1 = 1, x2 = 1, y1 = 4, y2 = 1;
let resultadoX, resultadoY, resultado, raiz = Math.sqrt(resultado);
resultadoY = Math.pow(y1 - y2, 2);
resultadoX = Math.pow(x1 - x2 , 2);
resultadoX = Math.sqrt(resultadoX);
resultadoY = Math.sqrt(resultadoY);
resultado = resultadoX + resultadoY;
console.log(resultado);