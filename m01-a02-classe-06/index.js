let p = 76; // Declara peso da pessoa em kg
const a = 1.79 // Declara a altura da pessoa em metros

imc = p / Math.pow(a,2);  // Calcula o imc da pessoa

console.log (imc); // Imprime o peso da pessoa