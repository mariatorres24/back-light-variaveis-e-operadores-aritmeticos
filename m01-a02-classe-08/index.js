const a = 27;
const b = 123;
const c = 50;
const d = (c * b) / a;
let desc = d.toFixed(2);

console.log(
  `Desconto necessário é de ${desc}%. O que é uma quantidade bem estranha para um desconto, mas é isso aí!`
);
