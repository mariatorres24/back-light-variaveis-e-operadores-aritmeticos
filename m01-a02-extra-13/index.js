const raio = 23;
const C = (2 * Math.PI * raio).toFixed(0);
const Ao = ((Math.PI * raio) ^ 2).toFixed(0);

console.log(
  `Para um raio de ${raio} UM, temos o comprimento de ${C} e área de ${Ao} `
);
