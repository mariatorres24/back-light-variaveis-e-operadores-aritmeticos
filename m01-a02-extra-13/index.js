C = 0, A = 0, r = 5;  // C = Cumprimento da circunferência - A  = área da círculo - r = raio

A = Math.PI * Math.pow(r, 2);
C = 2 * Math.PI * r;
console.log(`A área deste círculo é ${A} cm²`);
console.log(`O cumprimento deste círculo é ${C} cm`);

