let C = 0;
let Ao = 0;
let raio = 5;  

Ao = Math.PI * Math.pow(raio, 2);
C = 2 * Math.PI * raio;
console.log(`A Área do círculo é de ${Ao.toFixed(2)} cm²`);
console.log(`O Cumprimento do círculo é de ${C.toFixed(2)} cm`)