//Distance between two points

//Data Input
let x1 = 1, y1 = 1; //coordenates of the 1st point

let x2 = 1, y2 = 4; //coordenates of the 2nd point

//Formula of the distance between 2 points : D
let D = Math.sqrt ( Math.pow((x2-x1),2) + Math.pow(y2-y1),2));

//Print D
console.log(`The distance between the points of coordenates (${x1},${y1}) and (${x2},${y2}) is ${D} u.c..)`);
